package com.automation;

import org.openqa.selenium.By;
import org.openqa.selenium.WebDriver;

public class web {
    private By locatorweb = By.xpath("//*[contains(text(), 'Popular')]");
    private By locatorweb = By.xpath("//*[contains(text(), 'Best Sellers')]");

    private final WebDriver driver;


    public web(WebDriver driver) {
        this.driver = driver;
    }

    public void {
    driver.findElement().sendKeys
}
